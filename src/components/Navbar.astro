---
let open = true;
---

<div class="top-bar">
    <button class="menu-button" onclick={open = !open}>☰</button>
    <div id="btnbar" class="top-bar-buttons">
        <button class="top-bar-button">About Us</button>
        <button class="top-bar-button">Gurkenland</button>
        <button class="top-bar-button">KeinPLanGrad</button>
    </div>
</div>

<style>
    .top-bar {
        position: fixed;
        top: 0;
        left: 0;
        width: {open ? "100%" : "0%"};
        height: 40px;
        background-color: rgb(var(--accent));
        display: flex;
        align-items: center;
        justify-content: flex-start;
        padding: 0 20px;
        box-sizing: border-box;
        transition: 0.5s;
    }
    .menu-button {
        background: none;
        border: none;
        color: white;
        font-size: 18px;
        cursor: pointer;
        margin-right: 50px;
    }

    .top-bar-buttons {
        display: flex;
        align-items: center;
    }

    .top-bar-button {
        background: none;
        border: none;
        color: white;
        font-size: 16px;
        cursor: pointer;
        margin-right: 0px;
    }
</style>
